export type ServiceData = {
  title: string;
  description: string;
  img: string;
};

export type ProjectData = {
  title: string;
  description: string;
  img: string;
  labels: string[];
  company: string;
};
